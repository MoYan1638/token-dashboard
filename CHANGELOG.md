# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-09-21

界面动效与质感增强。**不引入任何第三方依赖，页面依旧零外部请求。**

### 新增

- **暗色模式** — 跟随系统 `prefers-color-scheme`，全套配色变量自动切换，
  图表、提示条、悬浮态一并适配
- **KPI 数字滚动** — 数值从 0 缓动到目标值，千分位与「万/亿」单位保持正确
- **占比条与柱状图生长动画** — 从零尺寸展开到实际值
- **卡片错峰入场** — 各区块依次淡入上移，只播一次
- **卡片指针光晕** — 桌面端鼠标划过 KPI 时跟随位置的高光
- **手动刷新按钮** — 右上角一键重新扫描，带旋转反馈
- **数字等宽对齐** — `font-variant-numeric: tabular-nums`，表格数字列不跳动

### 变更

- 标题区新增图标标识，刷新按钮与数据时间徽章并入同一操作区

### 实现要点

所有动效一律采用**渐进增强**：CSS 里默认就是最终可见态，只有脚本成功执行
并给 `<html>` 加上 `js-anim` 后，才会启用动画初始态。因此无 JS、脚本报错
或直接抓取静态文件时，页面内容始终完整可见，不会白屏。

同时尊重 `prefers-reduced-motion`：用户开启减少动效后，所有动画直接跳过。

## [1.0.0] - 2026-09-21

首个公开版本。

### 新增

- **多 Agent 日志自动探测** — 覆盖 WorkBuddy、Claude Code、Codex CLI、OpenClaw、
  Cursor、Gemini CLI、Continue、Aider、Cline、Roo Code、Windsurf、Kilo Code 共 12 种来源
- **通用解析器** — 采用「候选键名 + 递归下钻」策略，不绑定具体 schema；
  输入/输出/缓存/推理/模型/时间各有候选键名集合，支持 jsonl 与整份 JSON
- **实时看板** — 内置本地 HTTP 服务，页面每次请求即时扫描，默认 15 秒自动刷新
- **静态快照** — `scan` 子命令输出单文件 HTML，便于分享与存档
- **多维度统计** — 总览 KPI、来源对比、模型分布、每日趋势、会话 Top 20
- **费用估算** — 内置价格表，支持配置文件覆盖单价
- **响应式布局** — 电脑横屏与手机竖屏自适应，窄屏下表格转为卡片式
- **JSON API** — `/api/data` 返回结构化摘要，`/health` 用于健康检查
- **零依赖** — 仅使用 Python 标准库，无需安装任何第三方包
- **完全离线** — 不发起任何网络请求，数据不出本机

### 说明

- 费用为按公开单价折算的估算值，实际账单以服务商为准
- 页面上以 `~` 替代用户目录，避免分享截图时暴露用户名
