# 统一换行符为 LF，避免不同系统产生整文件 diff 噪音
* text=auto eol=lf

# Windows 批处理必须保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.zip binary
*.woff binary
*.woff2 binary
